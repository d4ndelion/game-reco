package com.dandelion.gamereco.utils.errors

import androidx.annotation.StringRes
import com.dandelion.gamereco.R

enum class ERRORS(@StringRes val stringId: Int) {
    LOG_IN(R.string.error_log_in);
}
